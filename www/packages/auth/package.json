{
  "name": "@zeyaddeeb/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "format": "biome format .",
    "lint": "biome lint .",
    "check": "biome check .",
    "generate": "bunx @better-auth/cli generate --config script/auth-cli.ts --output ../db/src/schema/auth/generated.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@zeyaddeeb/db": "workspace:*",
    "better-auth": "^1.6.11",
    "zod": "^4.4.3"
  },
  "peerDependencies": {
    "react": "^19"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@zeyaddeeb/config": "workspace:*",
    "typescript": "catalog:"
  }
}
