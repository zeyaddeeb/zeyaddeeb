$schema: "https://moonrepo.dev/schemas/workspace.json"

telemetry: false

vcs:
  provider: "github"
  defaultBranch: "main"
  hooks:
    pre-commit:
      - "moon sync projects"
    pre-push:
      - "moon sync projects"

projects:
  root: "."
  www: "www"
  robot: "robot"
  crdt: "crdt"
